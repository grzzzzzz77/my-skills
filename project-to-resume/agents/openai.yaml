interface:
  display_name: "项目转简历"
  short_description: "先确认投递方向，再基于代码证据生成 STAR 项目亮点"
  brand_color: "#2563EB"
  default_prompt: "Use $project-to-resume. First ask or explicitly confirm which role direction I am applying for in this invocation; never inherit a prior direction. Then analyze the local repository and generate target-role-specific, evidence-backed STAR resume highlights with three-round scoring, strict semantic checks, safe-bullet provenance, anchor-linked logic chains, and interview deep dives."
